# In, On, and At for Place

The user correctly chose `in`, `on`, and `at` in five physical-location examples after using the container, surface, and point/location images. Workplace usage remains a later nuance because some sentences can use more than one preposition depending on meaning.
